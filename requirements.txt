# Core FastAPI dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Existing dependencies
Flask>=2.3.0
PyGithub>=1.59.0
aiohttp>=3.9.0
httpx
loguru
marshmallow
psutil
pyyaml
questionary
requests>=2.31.0
rich
python-dotenv
textual

# Gunicorn is only needed for production server
# (comment out if not deploying with Gunicorn)
gunicorn

# LlamalyticsHub requirements
# (add or update aiohttp version if present) 